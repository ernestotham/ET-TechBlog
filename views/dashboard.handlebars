<!-- Loop over each object in the projects array -->
<h1>Your Dashboard</h1>
<br>
<br>
<div class="container border border-success rounded-3 ">
{{#each posts as |post| }}

    <br><br>
    <div class="card border border-success">
        <div class="card-header" id="{{post.id}}">
            {{post.post_title}} Posted by {{post.user.name}} on: {{format_date post.updated_on }}
        </div>
        {{!-- <div class="card-body">
            <div class="mb-3">
                <label for="exampleFormControlTextarea1" class="form-label"></label>
                <textarea readonly class="form-control" id="" rows="3">{{post.post_body}}</textarea>
            </div>

        </div> --}}
    </div>
    <BR></BR>


    {{/each}}




    <form method="get" action="/addpost">
        <BR></BR>
        <div class="d-grid gap-2">
            <button class="btn btn-primary" type="submit">+New Post</button>
        </div>
        <BR></BR>
        <BR></BR>
    </form>


</div>


<br><br>

<script src="./scripts/modPost.js"></script>